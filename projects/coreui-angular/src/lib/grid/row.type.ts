export type NumberOfColumns = number | 'auto';

export interface IRow {
  xs?: NumberOfColumns;
  sm?: NumberOfColumns;
  md?: NumberOfColumns;
  lg?: NumberOfColumns;
  xl?: NumberOfColumns;
  xxl?: NumberOfColumns;
}
